#include "Physicsworld.h"

void PhysicsWorld::AddToWorld(RigidBody* rigidBody)
{
	if (rigidBody == nullptr)
		return;

	// Check if is in world..
}

// Removes a rigid body from the PhysicsWorld
// rigidBody can be passed as nullptr
void PhysicsWorld::RemoveFromWorld(RigidBody* rigidBody)
{
	if (rigidBody == nullptr)
		return;

	// Check if is not in world..
}